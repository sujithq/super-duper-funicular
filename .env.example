# SolarScope CLI Environment Configuration
# Copy this file to .env and fill in your values

# GitHub Personal Access Token for AI features
# Create at: https://github.com/settings/tokens (with 'models:read' permission)
GITHUB_TOKEN=github_pat_test

# Optional: Custom data file path (if different from default)
# SOLARSCOPE_DATA_FILE=/path/to/your/solar-data.json
